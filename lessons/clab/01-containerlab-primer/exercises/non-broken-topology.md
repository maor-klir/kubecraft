# Deliverables

## Explanation of the difference between a node being "running" and an interface being "up"

- A node running means the OS has been initialized and is successfully booted up.  
- A network interface being in an "up" state means it's administratively enabled and a physical/virtual link exists connecting it to another node - veth pairs in containerlab's case.
